<div class="container">
    <div class="d-flex justify-content-center">
        <h1> {{firstName}} & {{spouseName}} </h1>
    </div>
</div>

{{#each guests as |guest|}}
<div class="dish col-md-5">
  <p>
    {{guest.id}}
    <br>
    Guest Name: {{guest.guestName}}
  </p>
  <p>
    Food Preference: {{guest.foodChoice}}
  </p>
</div>
{{/each}}
</div>
<script src="/js/add-guest.js"></script>